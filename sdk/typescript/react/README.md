# @supaapps/platform93-react

React providers and hooks for headless Platform93 authentication.

```bash
npm install @supaapps/platform93-react
```

This package uses `@supaapps/platform93-auth` and supports React 18.3 and React 19.
