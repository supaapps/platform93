# @supaapps/platform93-events

Platform93 CloudEvents contracts, bundled event schemas, and webhook signature
verification.

```bash
npm install @supaapps/platform93-events
```

Custom application events remain typed as application-defined JSON while built-in
events use the bundled versioned contracts.
